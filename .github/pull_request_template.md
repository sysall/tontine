## What changes

<!-- A line or two, and why. -->

## Checks

- [ ] Changeset added (`npm run changeset`), or nothing user-facing changed
- [ ] `npm run typecheck` passes
- [ ] Targets `alpha` (only a release PR targets `main`)
