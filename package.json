{
  "name": "tontine-express-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo Tontine Express - Mobile App & Unified NestJS API",
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "dev:api": "npm run start:dev --workspace=services/api",
    "dev:mobile": "npm run start --workspace=apps/mobile",
    "dev": "npm run dev:api",
    "db:push": "npm run prisma:push --workspace=services/api",
    "db:generate": "npm run prisma:generate --workspace=services/api",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "prisma:generate": "npm run prisma:generate --workspace=services/api",
    "prisma:push": "npm run prisma:push --workspace=services/api",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspace=services/api",
    "changeset": "changeset",
    "version": "changeset version && npm install --package-lock-only",
    "release": "node scripts/tag-release.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10"
  },
  "keywords": [
    "tontine",
    "fintech",
    "senegal",
    "expo",
    "react-native",
    "nestjs"
  ],
  "author": "Tontine Express Team",
  "license": "MIT",
  "dependencies": {
    "@expo/config-plugins": "~8.0.0"
  }
}
