# Copy to .env and point it wherever your API is running. Only what the dev
# server picks up — a built APK takes its URL from the eas.json build profile.
#
#   http://localhost:3000/api/v1     iOS simulator or web
#   http://10.0.2.2:3000/api/v1      Android emulator reaching your machine
#   https://<your-tunnel>/api/v1     a phone reaching your machine (ngrok)
#
# Leave it out to use the deployed API.
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
